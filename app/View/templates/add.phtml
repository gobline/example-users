<?php
if ($model->success) {
    $route('info')->params('id/'.$model->id)->redirect();
}

$pageTitle = 'Add User';
$title($pageTitle);
?>

<h1><?= $pageTitle ?></h1>

<?php
if ($model->maxNbUsersReached) {
    echo $render(__DIR__.'/partial/max-users-reached.phtml');
    return;
}
?>

<?= $render(__DIR__.'/partial/form.phtml', ['model' => $model, 'buttonText' => 'Add' ]) ?>
